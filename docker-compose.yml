version: '3'
services:
  postgres:
    image: 'postgres:latest'
    container_name: 'postgres-db'
    env_file:
    - .env
    volumes:
    - ${PWD}/data/postgres:/data/postgres
    ports:
      - '5432:5432'